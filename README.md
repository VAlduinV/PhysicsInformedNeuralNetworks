# Лабораторна робота №4  
**Фізично-інформовані нейронні мережі (Physics-Informed Neural Networks, PINN)**

Цей проєкт показує, як використати PINN для розв’язання задачі теплопровідності на реальному наборі даних з Kaggle.  
Мережа одночасно:

1. **Підганяє дані** (температуру `T(x, t)` з CSV);
2. **Дотримується фізики**, заданої рівнянням теплопровідності:

$$
\frac{\partial u}{\partial t} = \alpha \frac{\partial^2 u}{\partial x^2}.
$$

---

## 1. Структура проєкту

```text
PhysicsInformedNeuralNetworks/
│
├─ data/
│   ├─ data.csv             # повний датасет
│   ├─ data_sampled.csv     # підвибірка даних
│   └─ img/                 # сюди зберігаються графіки (створюється автоматично)
│
├─ config.py                # налаштування, гіперпараметри
├─ data_utils.py            # завантаження та підготовка даних
├─ pinn_model.py            # архітектура PINN
├─ physics.py               # фізичний (PDE) лосс
├─ trainer.py               # навчання та оцінка моделі
├─ visualization.py         # збереження графіків
└─ main.py                  # точка входу (основний запуск)
```

Усі результати аналізу для звіту зібрані в окремому файлі:  
👉 **[TECHNICAL_ANALYSIS.md](TECHNICAL_ANALYSIS.md)**

---

## 2. Вимоги

- Python 3.10+ (рекомендовано 64-bit)
- Створене віртуальне середовище (опційно, але бажано)
- Встановлені бібліотеки:

```bash
pip install tensorflow numpy pandas matplotlib
```

(За потреби можна додати `requirements.txt` і зробити `pip install -r requirements.txt`.)

---

## 3. Підготовка даних

У папці `data/` мають лежати два файли у форматі CSV:

- `data.csv`
- `data_sampled.csv`

**Структура обох файлів однакова** – три колонки:

```text
x,t,T
```

- `x` — просторові координати;
- `t` — час;
- `T` — температура (цільова величина, яку апроксимує PINN, тобто `u(x, t)`).

---

## 4. Як запустити

1. Перейти в папку проєкту:

   ```bash
   cd PhysicsInformedNeuralNetworks
   ```

2. (Опційно) активувати віртуальне середовище:

   ```bash
   .venv\Scripts\activate        # Windows
   # або
   source .venv/bin/activate       # Linux / macOS
   ```

3. Встановити залежності (якщо ще не встановлені):

   ```bash
   pip install tensorflow numpy pandas matplotlib
   ```

4. Запустити головний файл:

   ```bash
   python main.py
   ```

Скрипт **послідовно** запустить експерименти для:

- `data_sampled.csv` з префіксом `sampled`;
- `data.csv` з префіксом `full`.

Для кожного запуску в консолі виводяться:

- значення `total loss`, `data loss`, `physics loss` по епохах;
- фінальний **Test MSE**.

Графіки автоматично зберігаються в `data/img/`:

- `sampled_loss.png`, `sampled_prediction.png`;
- `full_loss.png`, `full_prediction.png`.

### Приклад отриманих графіків

> Ці зображення з’являться після першого запуску `main.py`, якщо в наявності дані.

- Лосси для підвибірки:

  ![PINN training loss (sampled)](data/img/sampled_loss.png)

- Прогноз vs істина для підвибірки:

  ![True vs PINN prediction (sampled)](data/img/sampled_prediction.png)

- Лосси для повного датасету:

  ![PINN training loss (full)](data/img/full_loss.png)

- Прогноз vs істина для повного датасету:

  ![True vs PINN prediction (full)](data/img/full_prediction.png)

---

## 5. Що можна змінювати студентам

1. **Файл `config.py`**:
   - `NUM_EPOCHS` — кількість епох;
   - `LEARNING_RATE` — швидкість навчання;
   - `ALPHA` — коефіцієнт теплопровідності в PDE;
   - `NORMALIZE_INPUTS` — вкл/викл нормалізацію `x` та `t`.

2. **Файл `pinn_model.py`**:
   - кількість шарів (`num_hidden_layers`);
   - кількість нейронів (`hidden_units`);
   - типи активацій.

3. **Файл `physics.py`**:
   - форму фізичного рівняння (можна спробувати інший PDE/ODE);
   - структуру обчислення `residual`.

4. **Файл `trainer.py`**:
   - співвідношення між data loss і physics loss  
     (наприклад, `total_loss = data_loss + λ * physics_loss`).

---

## 6. Що потрібно додати у звіт (мінімальний набір)

У звіті (або окремому `REPORT.md`) студент повинен:

1. **Коротко описати теорію**:
   - що таке PINN;
   - чому total loss = data loss + physics loss;
   - яке саме PDE ми використовуємо (рівняння теплопровідності).

2. **Описати датасет**:
   - що означають колонки `x, t, T`;
   - звідки взято дані (посилання на Kaggle).

3. **Додати результати експериментів**:
   - вставити/прикріпити графіки:
     - `sampled_loss.png`, `sampled_prediction.png`;
     - `full_loss.png`, `full_prediction.png`;
   - навести значення Test MSE для обох варіантів (`sampled` і `full`).

4. **Короткий якісний аналіз**:
   - як поводяться криві втрат;
   - чи сильно відрізняються результати на малому й повному датасеті;
   - чи допомагає фізичний лосс (чи не “вимикається”).

5. **Власний експеримент** (обов’язково щонайменше один):
   - змінити **один** параметр (наприклад, `ALPHA`, кількість шарів/нейронів або коефіцієнт при physics loss);
   - перезапустити навчання;
   - показати, як змінилися графіки та Test MSE;
   - зробити короткий висновок.

Як орієнтир для оформлення технічних висновків можна використати файл  
👉 **[TECHNICAL_ANALYSIS.md](TECHNICAL_ANALYSIS.md)**.
